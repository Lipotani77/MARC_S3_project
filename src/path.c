//
// Created by lelia on 11/11/2024.
//

#include <string.h>
#include <stdlib.h>
#include <stdio.h>
#include "../include/path.h"


/*ht_path create_ht_path()
{
    ht_path path;
    path.head = NULL;
    path.tail = NULL;

    return path;

}

int isEmptyHtPath(ht_path path)
{
    return (path.head == NULL);
}

void insertTail(ht_path *path, t_node *node)
{
    if (isEmptyHtPath(*path))
    {
        path->tail = node;
        path->head = path->tail;
    }
    else
    {

        path->tail->next = node; //how to make a next with sons ???
        path->tail = node;
    }
}

*/
